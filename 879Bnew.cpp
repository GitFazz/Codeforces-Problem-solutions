#include<bits/stdc++.h>
using namespace std;
long long int i,j,k,n;
int main()
{
    cin>>n>>k;
    long long int a[n];
    map<long long int,long long int> iter;
    for(i=0;i<n;i++)
    {
        scanf("%lld",&a[i]);
    }
    sort(a,a[])

    return 0;
}
